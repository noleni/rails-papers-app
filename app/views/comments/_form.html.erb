<div class="p-3 booking-form">
  <%= simple_form_for [@post, comment] do |f| %>
    <%= f.input :content, label: false, placeholder: 'Ajouter votre commentaire ici', as: :text, autofocus: true, :input_html => {:style => 'width: 100%', :rows => 8, class: 'response-project'} %>
    <%= f.submit "Publier", class: "btn btn-flat" %>
  <% end %>
</div>
